mixin email-block(title, classList1)
    -if (classList1 === undefined) {classList1 = "";}
    div(class="email-block " + classList1)
        h3(class="email-block__title")= title
        input(class="email-block__field" type="email" placeholder="Email")